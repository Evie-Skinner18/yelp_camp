let numbers = [9, 6, 25, 200];

let nine = numbers[0];
let twentyFive = numbers[2];
let twoHundred = numbers[3];
let six = numbers[1];

// .length is a PROPERTY
let lengthOfArray = numbers.length;

// using the .log() method
// methods always end in parentheses
console.log(`The nine variable is ${nine}`);
console.log(`The twentyFive variable contains ${twentyFive}`);

// to-do: print to the console the length of our numbers array